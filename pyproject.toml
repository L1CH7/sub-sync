[project]
name = "subsync"
version = "0.1.0"
description = "Local Subscription & Header Relay Utility"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "qrcode[pil]>=8.0",
    "rich>=13.0.0",
    "cryptography>=42.0.0",
]

[tool.uv]
package = false
link-mode = "copy"
